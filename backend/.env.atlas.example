# Example env file for connecting the backend to MongoDB Atlas
# Copy to backend/.env.atlas (do not commit credentials)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.abcd123.mongodb.net/alm_db?retryWrites=true&w=majority
# Optional: allowed CORS origins (comma separated)
# CORS_ORIGINS=http://localhost:5173
